name = "sunshield-vision"
main = "index.js"
compatibility_date = "2025-01-01"

# The API key is NOT set here. Store it as a secret so it never
# lands in the repo:
#   wrangler secret put ANTHROPIC_API_KEY
